
import { GoogleGenAI, Modality } from "@google/genai";
import { ScriptBlock } from '../lib/n8nConverter';

export interface GenerationResult {
  botProfile: {
    voiceTone: string;
    keyCharacteristics: string[];
  };
  customizedScript: ScriptBlock[];
  visualizationPrompts: {
    scenario: string;
  };
}

const promptTemplate = `
Твоя роль: AI-ассистент, специализирующийся на бренд-интеграциях в чат-ботах.

**ЗАДАЧА:**
Проанализируй предоставленный сценарий чат-бота и информацию о партнёрских продуктах. Затем сгенерируй единый JSON-объект, содержащий:
1.  **Профиль голоса бота (botProfile):** Краткий анализ стиля общения бота.
2.  **Сценарий интеграции (customizedScript):** Набор логических блоков для диалога, которые органично рекомендуют партнёрский продукт в стиле бота.
3.  **Промпт для визуализации (visualizationPrompts):** Текстовый промпт на английском языке для генерации иллюстрации к сценарию, соответствующей стилю бота.

**ВХОДНЫЕ ДАННЫЕ:**

1.  **Сценарий чат-бота для анализа стиля:**
    \`\`\`
    {{BOD_SCRIPT}}
    \`\`\`

2.  **Информация о партнёрских продуктах:**
    \`\`\`
    {{PARTNER_PRODUCTS}}
    \`\`\`

**ИНСТРУКЦИИ ПО ГЕНЕРАЦИИ:**

1.  **Анализ стиля:** Внимательно изучи сценарий. Определи тон голоса (формальный, дружелюбный, юмористический), ключевые слова, использование эмодзи, длину предложений.

2.  **Создание сценария интеграции (customizedScript):**
    *   Текст должен быть написан ТОЧНО в том же стиле, который ты определил на шаге 1. Это должна быть нативная рекомендация, а не реклама.
    *   Сценарий должен быть в формате массива **блоков**. Каждый блок - это логическая ветка диалога, которая запускается определённым триггером.
    *   Каждый блок должен иметь уникальный \`id\` (просто строка для идентификации), \`trigger\` (значение \`callback_data\` из кнопки или \`/start\` для начала диалога) и массив \`messages\` (сообщения внутри этого блока).
    *   Если в блоке несколько сообщений, они будут показаны пользователю последовательно.
    *   Структура сообщения: \`{"text": "...", "buttons": [[{"text": "...", "url": "...", "callback_data": "..."}]]}\`.
    *   **ВАЖНО:** Создай как минимум два блока: один для \`/start\` и один для ответа на кнопку с \`callback_data\`.

3.  **Создание промпта для визуализации (visualizationPrompts):**
    *   **scenario:** Создай промпт для генерации иллюстрации, которая визуально представляет созданный тобой сценарий интеграции. Промпт должен быть на английском языке.

**ФОРМАТ ВЫВОДА:**
Верни ТОЛЬКО валидный JSON-объект. Никаких объяснений, комментариев или \`\`\`json\`\`\` обёрток.
**КРИТИЧЕСКИ ВАЖНО:** Всегда экранируй двойные кавычки (") внутри строковых значений JSON с помощью обратного слэша (\\"). Например: \`"text": "Привет, \\"бро\\"!"\`.

**СТРУКТТУРА JSON-ОБЪЕкта:**
\`\`\`json
{
  "botProfile": {
    "voiceTone": "Дружелюбный и немного шутливый",
    "keyCharacteristics": ["Использует эмодзи 🌊 и 🏔️", "Обращается на 'ты'", "Предложения короткие и ясные"]
  },
  "customizedScript": [
    {
      "id": "start_recommendation",
      "trigger": "/start",
      "messages": [
        {
          "text": "Кстати, раз уж мы говорим о путешествиях, мои друзья сделали супер-удобный рюкзак Wanderer! 🎒 Идеален для гор. Хочешь посмотреть?",
          "buttons": [
            [
              {"text": "Да, интересно!", "url": "https://example-shop.com/wanderer-35l?ref=bot123"},
              {"text": "Не сейчас", "callback_data": "decline_wanderer_backpack"}
            ]
          ]
        }
      ]
    },
    {
       "id": "decline_response",
       "trigger": "decline_wanderer_backpack",
       "messages": [
          { "text": "Понял, не вопрос! 👍 Может, тогда заинтересует страховка для путешествий?" }
       ]
    }
  ],
  "visualizationPrompts": {
    "scenario": "Digital illustration of a cheerful cartoon backpack sitting on top of a mountain peak, waving a small flag. The style is clean, modern, and friendly, suitable for a chatbot interface."
  }
}
\`\`\`
`;

const regenerateMessagePromptTemplate = `
Твоя роль: Редактор и копирайтер для чат-ботов.

**ЗАДАЧА:**
Тебе нужно переписать ОДНО конкретное сообщение в уже существующем диалоге. Твоя цель — предложить альтернативную формулировку, сохраняя при этом первоначальный смысл, контекст диалога и, самое главное, уникальный стиль общения бота.

**КОНТЕКСТ:**

1.  **Оригинальный стиль бота (определён на основе этого скрипта):**
    \`\`\`
    {{BOT_SCRIPT}}
    \`\`\`

2.  **Рекомендуемые продукты (для понимания цели):**
    \`\`\`
    {{PARTNER_PRODUCTS}}
    \`\`\`

3.  **Полный сгенерированный диалог (для понимания места сообщения в беседе):**
    \`\`\`json
    {{CURRENT_SCRIPT}}
    \`\`\`

4.  **КОНКРЕТНОЕ СООБЩЕНИЕ, КОТОРОЕ НУЖНО ПЕРЕПИСАТЬ:**
    \`\`\`
    {{MESSAGE_TO_REGENERATE}}
    \`\`\`

**ИНСТРУКЦИИ:**
1.  Проанализируй тон голоса из **Оригинального стиля бота**.
2.  Пойми контекст из **Полного сгенерированного диалога**.
3.  Перепиши **Конкретное сообщение**, чтобы оно звучало по-новому, но оставалось верным стилю и смыслу.
4.  Не меняй никакие другие части диалога.

**ФОРМАТ ВЫВОДА:**
Верни ТОЛЬКО новый текст для этого сообщения. Никаких JSON, никаких объяснений, только одна строка с новым текстом.
`;


export async function* generateRecommendationsStream(
  botScript: string,
  partnerProducts: string,
  apiKey: string,
  temperature: number,
): AsyncGenerator<string, void, undefined> {
  if (!apiKey) {
    throw new Error("API ключ не предоставлен.");
  }
  const ai = new GoogleGenAI({ apiKey });
  const model = 'gemini-2.5-flash';
  
  const prompt = promptTemplate
    .replace('{{BOD_SCRIPT}}', botScript)
    .replace('{{PARTNER_PRODUCTS}}', partnerProducts);

  try {
    const response = await ai.models.generateContentStream({
        model: model,
        contents: prompt,
        config: {
            temperature: temperature,
            responseMimeType: "application/json",
        }
    });

    for await (const chunk of response) {
      yield chunk.text;
    }

  } catch (error: any) {
    console.error("Error calling Gemini API:", error);
    if (error.message && error.message.includes('API key not valid')) {
      throw new Error("Предоставлен неверный API ключ. Пожалуйста, проверьте его и попробуйте снова.");
    }
    throw new Error("Не удалось получить ответ от Gemini API. Проверьте правильность ключа и подключение к сети.");
  }
}


export async function* regenerateSingleMessageStream(
  botScript: string,
  partnerProducts: string,
  currentScript: ScriptBlock[],
  messageToRegenerate: string,
  apiKey: string,
  temperature: number,
): AsyncGenerator<string, void, undefined> {
    if (!apiKey) {
        throw new Error("API ключ не предоставлен.");
    }
    const ai = new GoogleGenAI({ apiKey });
    const model = 'gemini-2.5-flash';

    const prompt = regenerateMessagePromptTemplate
        .replace('{{BOT_SCRIPT}}', botScript)
        .replace('{{PARTNER_PRODUCTS}}', partnerProducts)
        .replace('{{CURRENT_SCRIPT}}', JSON.stringify(currentScript, null, 2))
        .replace('{{MESSAGE_TO_REGENERATE}}', messageToRegenerate);

    try {
        const response = await ai.models.generateContentStream({
            model: model,
            contents: prompt,
            config: {
                temperature: temperature,
            }
        });

        for await (const chunk of response) {
            yield chunk.text;
        }

    } catch (error: any) {
        console.error("Error calling Gemini API for message regeneration:", error);
        throw new Error("Не удалось перегенерировать сообщение.");
    }
}


export async function generateVisualizationImage(
  prompt: string,
  apiKey: string
): Promise<string> {
   if (!apiKey) {
      throw new Error("API ключ не предоставлен для генерации изображения.");
   }
   
   const ai = new GoogleGenAI({ apiKey });
   
   try {
     const response = await ai.models.generateContent({
        model: 'gemini-2.5-flash-image',
        contents: {
          parts: [{ text: prompt }],
        },
        config: {
            responseModalities: [Modality.IMAGE],
        },
      });

      for (const part of response.candidates[0].content.parts) {
        if (part.inlineData) {
          return part.inlineData.data; // return base64 string
        }
      }
      throw new Error("Изображение не было найдено в ответе API.");
   } catch (error: any) {
     console.error("Error generating image with Gemini:", error);
     throw new Error("Не удалось сгенерировать изображение. Пожалуйста, проверьте консоль.");
   }
}
